<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/splashView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_translucent_black">

    <ImageView
        android:id="@+id/splashImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:src="@drawable/bv_splash_blue_256"
        android:scaleType="fitCenter"/>

    <!-- TextView
        android:id="@+id/splashText"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="@dimen/splash_text_margin_bottom"
        android:gravity="bottom|center"
        android:shadowColor="@color/bg_translucent_black"
        android:shadowDx="-1"
        android:shadowDy="-1"
        android:shadowRadius="2"
        android:text="@string/user_guide_name"
        android:textColor="@color/opaque_white"
        android:textSize="20sp" / -->
</RelativeLayout>